from pathlib import Path

from apollo.container import ApolloContainer


def play_scenario(container: ApolloContainer, scenario_id: str, scenario: Path) -> Path:
    return Path()


def analyze_scenario(scenario_id: str, scenario: Path) -> None:
    pass
